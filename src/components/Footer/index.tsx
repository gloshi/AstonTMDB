import React from "react";
import styles from "../../styles/Footer/Footer.module.scss";
const Footer: React.FC = () => {
  return (
    <footer className={styles.container}>
      <h3 className={styles.text}>simple Footer</h3>
    </footer>
  );
};

export default Footer;
